import React from 'react';
import styled from "styled-components";

export const About = () => {
    return (
        <StyledAbout>

        </StyledAbout>
    );
};

const StyledAbout = styled.section`

`;